# Database (required)
DATABASE_URL=postgres://actio:actio@localhost:5433/actio

# HTTP server (optional)
HTTP_PORT=3000

# Python worker (optional)
WORKER_HOST=127.0.0.1
WORKER_PORT=50051

# LLM todo generation (optional — omit to disable)
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=sk-...
LLM_MODEL=gpt-4o-mini

# Logging (optional)
RUST_LOG=actio_asr=info
