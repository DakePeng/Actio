[workspace]
members = [
    "actio-core",
    "src-tauri",
]
resolver = "2"

# Build dependencies (llama-cpp, sherpa-onnx, etc.) with optimizations even in
# dev mode.  Without this, CPU inference is 50-100x slower than release.
[profile.dev.package."*"]
opt-level = 3

