.worktrees/
.claude/
.codex/
.gemini/

# Python cache artifacts
__pycache__/
*.py[cod]
.superpowers
.omx
.omc

# Agent-generated files
.agents/
AGENTS.md
**/AGENTS.md
skills-lock.json

# Crash dumps
*.stackdump

# SQLite database
*.db
*.db-wal
*.db-shm

# ML models (downloaded at runtime)
**/models/*.onnx
**/models/*.tar.bz2

# Bundled assets — the VAD is shipped inside the binary via include_bytes!
# so it must be tracked. Negate the broader **/models/*.onnx rule above.
!backend/actio-core/assets/*.onnx

# Spike directory (throwaway, scratch Rust workspace)
spike/
